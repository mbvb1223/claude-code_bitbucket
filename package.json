{
  "name": "claude-bitbucket-review",
  "version": "1.0.0",
  "description": "Simplified PR review using Claude CLI with Bitbucket integration",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.10",
    "typescript": "^5.6.2"
  },
  "license": "MIT"
}
